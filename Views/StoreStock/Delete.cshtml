@{
    ViewData["Title"] = "Delete Store Stock";
}

@model StoreStock

<div class="row">
    <div class="col-md-6">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <h5 class="card-title mb-0">Confirm Delete</h5>
            </div>
            <div class="card-body">
                <p>Are you sure you want to delete this store stock entry?</p>
                <div class="mb-3">
                    <strong>Store Code:</strong> @Model.StCd
                </div>
                <div class="mb-3">
                    <strong>Major Category:</strong> @Model.MajCat
                </div>
                <div class="mb-3">
                    <strong>Stock Qty:</strong> @Model.StkQty?.ToString("F2")
                </div>

                <form asp-action="DeleteConfirmed" method="post" class="d-inline">
                    <input type="hidden" asp-for="Id" />
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Delete
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</div>
