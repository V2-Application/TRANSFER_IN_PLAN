@{
    ViewData["Title"] = "Edit Bin Capacity";
}

@model BinCapacity

<div class="row">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">Edit Bin Capacity</h5>
            </div>
            <div class="card-body">
                <form asp-action="Edit" method="post">
                    <input type="hidden" asp-for="Id" />

                    <div class="mb-3">
                        <label asp-for="MajCat" class="form-label">Major Category</label>
                        <input asp-for="MajCat" class="form-control" />
                        <span asp-validation-for="MajCat" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="BinCapDcTeam" class="form-label">Bin Capacity DC Team</label>
                        <input asp-for="BinCapDcTeam" type="number" step="0.01" class="form-control" />
                        <span asp-validation-for="BinCapDcTeam" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="BinCap" class="form-label">Bin Capacity</label>
                        <input asp-for="BinCap" type="number" step="0.01" class="form-control" />
                        <span asp-validation-for="BinCap" class="text-danger"></span>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-success">
                            <i class="bi bi-check-circle"></i> Save Changes
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
